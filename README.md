# Controller Profile Encoder & Decoder

This Node.js script packs 19 complex controller configurations (booleans, custom range integers, and a 4-digit patch version) into a highly compressed, 12-digit URL-safe Base64 ASCII string.
